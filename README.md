## Use tf.estimaor to learn a binary classifier on census data (available from UC Irvine)
`tf.data` is used for pipeline building.
Learnt estimator (classifier) is saved and served through `tf.serving`. 
